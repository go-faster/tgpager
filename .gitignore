# Build output
/dist/
/tgpager

# Runtime state: session is a credential, caches are per-account
session.json
*.bolt

# Local audio
tone.ogg
*.ogg

# Editors
.idea/
.vscode/

# Docker deployment inputs, which are local and often secret.
deploy/secrets/
deploy/alert.ogg
