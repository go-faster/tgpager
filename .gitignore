# Build output
/dist/
/tgpager

# Runtime state: session is a credential, caches are per-account
session.json
*.bolt

# Local audio
tone.ogg
*.ogg

# Editors
.idea/
.vscode/

# Docker deployment inputs, which are local and often secret.
deploy/secrets/
deploy/alert.ogg
# Model weights, fetched per the README rather than vendored.
deploy/glados/model_assets/
# Local compose overrides, which carry an app_id and a peer.
deploy/*.local.yml
deploy/bot-voice.yml
